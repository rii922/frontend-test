import * as React from 'react';
import Header from './Header';
import { Link } from 'react-router-dom';

const ArticlePage: React.FC = () => {
  return (
    <>
      <Header title="Qiita 記事詳細" />
    </>
  );
};

export default ArticlePage;
